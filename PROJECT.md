# 합본 제작 자동화 프로젝트

담당: 이병규 PM (창조공작소 사운드팀)

---

## 프로젝트 목적

게임 더빙 대본 합본 제작 자동화.

고객사에서 받은 xlsx 대본 파일들이 담긴 폴더를 지정하면, AI가 각 파일/시트를 타입별로 분류하고, 타입별 매뉴얼을 참조해서 처리한 뒤, 표준 합본 xlsx 1개와 로그 md 하나를 생성한다.

사용 대상: 창조공작소 사운드팀 전원

---

## 왜 순수 Python 자동화가 안 되는가

고객사마다 대본 형식이 완전히 제각각이다. 같은 프로젝트 내에서도 헤더 위치, 컬럼명, KR 컬럼 수, 캐릭터명 구조 등이 전부 다르다. 키워드 기반 규칙으로는 커버 불가능하며, AI가 매뉴얼을 참조해서 맥락을 보고 판단해야 한다.

---

## 역할 분리

- **AI**: 타입 분류 + 각 시트 해석 및 처리
- **Python**: 파일 읽기 / xlsx 출력 / 결과 조립

---

## 실행 환경

Python + Gemini API (경제적 이유 — 합본 1개 비용 수십 원 수준, 로컬 exe로 팀 전원 공유)

---

## 실행 흐름

```
PROJECT.md 읽기
   ↓
functions/classification_guide.md 참조
→ 각 파일/시트 타입 분류
→ 분류 불가 시트는 Log에 사유 기록 후 스킵
   ↓
types/Type_XXX.md 참조
→ 타입별 정리 (열 인식, 데이터 추출, 특수 처리)
   ↓
output/template_spec.md 참조
→ 정리된 시트들을 표준 합본으로 조립
   ↓
합본 xlsx, 로그 md 출력
```

---

## 입력 파라미터 (매 프로젝트마다)

| 파라미터 | 필수 여부 | 설명 |
|----------|----------|------|
| 프로젝트명 | 필수 | |
| 차수 | 선택 | 없으면 생략 |
| 녹음 언어 | 필수 | KR / EN / JP |
| 기준 옵티컬 언어 | 필수 | EN / CN / 없음 |
| 원본 폴더 경로 | 필수 | 하위 폴더 포함 수집 |
| 출력 경로 | 필수 | |

---

## 파일 구조

```
/합본자동화/
│
├── PROJECT.md                      ← 이 문서
│
├── functions/
│   ├── classification_guide.md     ← 타입 분류 기준
│   ├── column_inference.md         ← 열 추론 규칙 (패턴 기반)
│   └── type_management.md          ← 타입 추가/수정 절차
│
├── types/
│   ├── Type_메인.md
│   ├── Type_PV.md
│   ├── Type_전투.md
│   ├── Type_캐릭터음성.md
│   ├── Type_짧은음성.md
│   ├── Type_무한대.md
│   └── Type_명방캐릭터.md
│
└── output/
    └── template_spec.md            ← 합본 표준 조립 규칙
```

---

## 주요 확정 사항

- 수정사항 파일 등 애매한 파일은 사용자가 폴더에서 미리 제외한 후 실행
- 숨김 처리된 시트는 자동 스킵
- 한국어 대사가 없는 시트는 자동 스킵
- 파일 단위로 나눠져 있는 건 이유가 있으니 함부로 하나의 시트로 묶거나 합치지 않음
- 파일명 열이 여러 개일 경우 AI가 기준 파일명 열을 판단해서 선택, 나머지는 숨김 처리
- 시트명 변환 시 캐릭터명 중복 제거 (예: `PV_티티` → `티티_PV`)
