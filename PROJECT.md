# 합본 제작 자동화 프로젝트

담당: 이병규 PM (창조공작소 사운드팀)

---

## 프로젝트 목적

게임 더빙 대본 합본 제작 자동화.

고객사에서 받은 xlsx 대본 파일들이 담긴 폴더를 지정하면, AI가 각 파일/시트를 타입별로 분류하고, 타입별 매뉴얼을 참조해서 처리한 뒤, 표준 합본 xlsx 1개를 생성한다.

사용 대상: 창조공작소 사운드팀 전원

---

## 왜 순수 Python 자동화가 안 되는가

고객사마다 대본 형식이 완전히 제각각이다. 같은 프로젝트 내에서도 헤더 위치, 컬럼명, KR 컬럼 수, 캐릭터명 구조 등이 전부 다르다. 키워드 기반 규칙으로는 커버 불가능하며, AI가 매뉴얼을 참조해서 맥락을 보고 판단해야 한다.

---

## 역할 분리

- **AI**: 타입 분류 + 각 시트 해석 및 처리
- **Python**: 파일 읽기 / xlsx 출력 / 결과 조립

---

## 실행 환경

**Python + Gemini 2.5 Flash API**

로컬 exe로 팀 전원 공유. Gemini 2.0 Flash는 2026년 6월 1일 종료 예정으로 2.5 Flash 사용.
맥락 판단이 복잡한 작업(KR 컬럼 다중 판단, 파트 제목행 구별 등) 때문에 Lite 대신 Flash 채택.

### API 비용 추정 (Gemini 2.5 Flash 기준, 2026년 5월)

| 구분 | 단가 |
|------|------|
| 입력 토큰 | $0.30 / 1M |
| 출력 토큰 | $2.50 / 1M |

| 시나리오 | 입력 토큰 | 출력 토큰 | 합계(USD) | 합계(KRW) |
|----------|----------|----------|----------|----------|
| 소규모 (파일 8개) | ~70,000 | ~30,000 | ~$0.10 | **약 140원** |
| 대규모 (파일 15개) | ~210,000 | ~80,000 | ~$0.26 | **약 365원** |

> Batch API 사용 시 50% 할인 적용 가능 (실시간 처리 불필요한 경우)

---

## 실행 흐름

```
PROJECT.md 읽기
   ↓
functions/classification_guide.md 참조
→ 각 파일/시트 타입 분류
→ 분류 불가 시트는 Log에 사유 기록 후 스킵
   ↓
types/Type_XXX.md 참조
→ 타입별 정리 (열 인식, 데이터 추출, 특수 처리)
   ↓
output/template_spec.md 참조
→ 정리된 시트들을 표준 합본으로 조립
   ↓
합본 xlsx 출력
```

---

## 입력 파라미터 (매 프로젝트마다)

| 파라미터 | 필수 여부 | 설명 |
|----------|----------|------|
| 프로젝트명 | 필수 | |
| 차수 | 선택 | 없으면 생략 |
| 녹음 언어 | 필수 | KR / EN / JP |
| 기준 옵티컬 언어 | 필수 | EN / CN / 없음 |
| 원본 폴더 경로 | 필수 | 하위 폴더 포함 수집 |
| 출력 경로 | 필수 | |

---

## 파일 구조 및 각 파일의 역할

```
/합본자동화/
│
├── PROJECT.md
├── HANDOFF.md
│
├── functions/
│   ├── classification_guide.md
│   ├── column_inference.md
│   └── type_management.md
│
├── types/
│   ├── Type_메인.md
│   ├── Type_PV.md
│   ├── Type_전투.md
│   ├── Type_캐릭터음성.md
│   ├── Type_짧은음성.md
│   ├── Type_무한대.md
│   └── Type_명방캐릭터.md
│
└── output/
    └── template_spec.md
```

### PROJECT.md (이 문서)
프로젝트 전체 개요. 목적, 구조, 역할 분리, 실행 흐름, 각 파일의 역할을 설명한다.
새 대화를 시작할 때 가장 먼저 읽어야 하는 문서.

### HANDOFF.md
새 대화창에서 이전 작업을 이어받기 위한 인계 문서.
현재 진행 상황, 분석 완료된 파일 목록, 다음 할 일을 기록한다.
작업이 진행될수록 업데이트된다.

---

### functions/classification_guide.md
**역할: 타입 분류 기준**

대본 파일/시트가 주어졌을 때 어떤 타입인지 판단하는 기준을 담은 문서.
각 타입의 구조적 특징을 서술해두고, 주어진 시트와 비교해서 가장 가까운 타입으로 분류한다.
조건 → 타입 식의 룰베이스가 아니라, 특징 서술 → 비교 판단 방식으로 작동한다.
분류가 불가능한 경우 Log에 사유를 기록하고 스킵한다.

### functions/column_inference.md
**역할: 열 추론 규칙**

열 이름이 없거나 불명확한 경우, 셀 값의 형식과 패턴을 보고 열의 성격을 추론하는 규칙을 담은 문서.
classification_guide.md와 각 Type_XXX.md에서 필요 시 참조한다.
파일명 열 / 캐릭터명 열 / 대사 열 / 감정 열 / 타임코드 열 / duration 열 등 패턴별 추론 기준이 정리되어 있다.

### functions/type_management.md
**역할: 타입 추가/수정 절차**

새로운 대본 형식이 등장했을 때 타입을 추가하는 기준과 절차,
기존 타입을 수정하거나 통폐합할 때 어떤 문서를 건드려야 하는지를 정의한다.

---

### types/Type_XXX.md
**역할: 타입별 처리 매뉴얼**

classification_guide.md가 "이게 어떤 타입이다"를 판단하는 문서라면,
Type_XXX.md는 "그 타입으로 판단했으면 이제 어떻게 처리해라"를 알려주는 문서다.

각 Type 파일에 들어가는 내용:

1. **열 인식** — 이 타입에서 어떤 열을 찾아야 하는지. 캐릭터명/대사/파일명/감정/옵티컬 열을 어떻게 식별하는지. 헤더 이름이 다를 수 있으므로 column_inference.md를 참조해서 패턴으로 찾는 방법 포함.

2. **특수 처리 규칙** — 이 타입만의 처리가 필요한 것들. 예를 들어 병합셀 펼치기, 파트 제목행 건너뛰기, 여러 시트 합산, 파일명에서 캐릭터명 추출 등.

3. **추출 결과물** — 정리 후 어떤 데이터가 나와야 하는지. 표준 열 구조(파일명/캐릭터명/감정/대사/옵티컬)에 어떻게 매핑되는지.

4. **예외 케이스** — 이 타입 내에서 변형이 있을 수 있는 것들. ALT 열 유무, duration 유무, 헤더 위치 가변 등.

AI가 이 파일만 읽으면 해당 타입 대본을 처음 봐도 혼자 처리할 수 있을 정도로 작성한다.

---

### output/template_spec.md
**역할: 합본 표준 조립 규칙**

각 타입별 정리가 완료된 시트들을 하나의 합본 xlsx로 조립하는 규칙.
시트 순서, 탭 그룹핑, 시트명 규칙, 열 구조, 시각화 규칙, 피크 라인 표기, 타이밍 검수 표기 등을 정의한다.
이 단계에서 AI의 판단은 최소화되며, 정해진 규칙대로 합치는 것이 주 작업이다.

---

## 주요 확정 사항

- 수정사항 파일 등 애매한 파일은 사용자가 폴더에서 미리 제외한 후 실행
- 숨김 처리된 시트는 자동 스킵
- 한국어 대사가 없는 시트는 자동 스킵
- 파일 단위로 나눠져 있는 건 이유가 있으니 함부로 합치지 않음
- 예외: Type_전투는 보스/일반전투 시트를 합본에서 1탭으로 합산
- 파일명 열이 여러 개일 경우 AI가 기준 파일명 열을 판단해서 선택, 나머지는 숨김 처리
- 시트명 변환 시 캐릭터명 중복 제거 (예: `PV_티티` → `티티_PV`)
